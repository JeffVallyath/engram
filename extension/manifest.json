{
  "manifest_version": 3,
  "name": "engram cookie bridge",
  "version": "1.0.0",
  "description": "Pushes cookies for the sites you track to a locally-running engram, so it can ingest login-gated lecture videos.",
  "permissions": ["cookies", "storage", "alarms", "tabs"],
  "host_permissions": ["<all_urls>", "http://127.0.0.1/*"],
  "background": { "service_worker": "background.js" },
  "action": { "default_popup": "popup.html", "default_title": "engram cookie bridge" }
}
